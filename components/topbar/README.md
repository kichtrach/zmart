# Topbar component

Reusable ZMart topbar component.

- `js/topbar.js` renders page title, global search, notifications, profile menu, and hamburger integration.
- `css/topbar.css` owns topbar layout/responsive styling.
- `icons/topbar-icons.css` is the isolated icon layer. Font Awesome is used for icons.
